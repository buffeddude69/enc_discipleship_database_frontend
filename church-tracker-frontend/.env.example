# In production (Vercel), set this to your deployed backend's URL, e.g.
# https://church-tracker-backend.onrender.com
# Locally, leave it unset -- it defaults to http://127.0.0.1:8000
VITE_API_BASE_URL=
